version: '3.2'
services:
  php:
    build:
          context: ./apache-php
    volumes:
      - ~/.composer-docker/cache:/root/.composer/cache:delegated
      - ./:/var/www/boost-test:z
    ports:
      - '80:80'

